os.integrations.bhekthink.on('connected', (data) => {
    console.log('BhekThink connected:', data);
});

os.integrations.bhekthink.on('response', (data) => {
    console.log('AI response:', data);
});

os.integrations.bhekthink.on('image', (data) => {
    console.log('Image generated:', data.url);
});
